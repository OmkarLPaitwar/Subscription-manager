# render.yaml — Deploy SubSync AI on Render.com (Free Tier)
# Docs: https://render.com/docs/blueprint-spec

services:
  # ── Backend API ────────────────────────────────────────────────────────────
  - type: web
    name: subsync-ai-api
    runtime: node
    rootDir: server
    plan: free                       # Free tier (spins down after 15 min idle)
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false                  # Set manually in Render dashboard
      - key: JWT_SECRET
        generateValue: true          # Render auto-generates a secure value
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: JWT_EXPIRE
        value: 7d
      - key: JWT_REFRESH_EXPIRE
        value: 30d
      - key: CLIENT_URL
        sync: false                  # e.g. https://subsync-ai.vercel.app
      - key: OPENAI_API_KEY
        sync: false                  # Optional
      - key: EMAIL_HOST
        value: smtp.gmail.com
      - key: EMAIL_PORT
        value: 587
      - key: EMAIL_USER
        sync: false
      - key: EMAIL_PASS
        sync: false

  # ── Frontend (Static Site) ─────────────────────────────────────────────────
  # NOTE: Better to deploy frontend on Vercel (see vercel.json).
  # Keeping this here as a fallback option.
  - type: web
    name: subsync-ai-client
    runtime: static
    rootDir: client
    plan: free
    buildCommand: npm install && npm run build
    staticPublishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html     # SPA fallback for React Router
    envVars:
      - key: REACT_APP_API_URL
        sync: false                  # e.g. https://subsync-ai-api.onrender.com/api
